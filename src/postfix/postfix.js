/**
 * Add postfix to a text
 *
 * @param {string} text
 * @param {string} postfix
 * @returns {string}
 */
const addPostfix = (text, postfix = '') => text + postfix;

module.exports = {
  addPostfix
};
